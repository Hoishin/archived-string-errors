const assert = require('assert');
const stringErrors = require('../index.js');

describe('stringErrors', () => {
  it('has a test', () => {
    assert(false, 'stringErrors should have a test');
  });
});
